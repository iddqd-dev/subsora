version: '3.8'

services:
  db:
    image: postgres:14.8-alpine3.18
    hostname: db
    environment:
      POSTGRES_DB: "subsora"
      POSTGRES_USER: "postgres"
      POSTGRES_PASSWORD: "postgres"
      PGDATA: "/var/lib/postgresql/data/pgdata"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - subsora_network
    ports:
      - target: 5432
        published: 45432
        protocol: tcp
        mode: host
    deploy:
      restart_policy:
        condition: any
        delay: 5s
        max_attempts: 3
      replicas: 1
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U postgres -d subsora" ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
  

  backend:
    image: cicd.zerity.ru:55000/subsora/backend:dev
    pid: host
    ports:
      - target: 8000
        published: 48000
        protocol: tcp
        mode: host
    volumes:
      - xray_config:/etc/xray_shared
      - /var/run/docker.sock:/var/run/docker.sock
    configs:
      - source: backend_env_config
        target: /app/.env
        mode: 0444
    environment:
      - XRAY_CONTAINER_NAME=subsora-xray
      - XRAY_CONFIG_PATH=/etc/xray_shared/config.json
    deploy:
      endpoint_mode: dnsrr
      restart_policy:
        condition: any
        delay: 5s
        max_attempts: 3
      update_config:
        parallelism: 1
        delay: 10s
        failure_action: rollback
        order: stop-first
    depends_on:
      - db
    command: ["sh", "-c", "echo 'Waiting for database...' && until nc -z db 5432; do echo 'Database not ready, waiting...'; sleep 2; done && echo 'Database is ready!' && alembic -c /app/backend/alembic.ini upgrade head && uvicorn main:app --host 0.0.0.0 --port 8000"]
    networks:
      - subsora_network

  bot:
    image: cicd.zerity.ru:55000/subsora/bot:dev
    configs:
      - source: bot_env_config
        target: /app/.env
        mode: 0444
    deploy:
      restart_policy:
        condition: any
    depends_on:
      - backend
    networks:
      - subsora_network

  frontend:
    image: cicd.zerity.ru:55000/subsora/frontend:dev
    ports:
      - target: 80
        published: 48080
        protocol: tcp
        mode: host
      - target: 443
        published: 48443
        protocol: tcp
        mode: host
    deploy:
      restart_policy:
        condition: any
    networks:
      - subsora_network

  nginx-proxy:
    image: nginx:1.25-alpine
    ports:
      - target: 80
        published: 48081
        protocol: tcp
        mode: host
    volumes:
      - ./nginx-proxy.conf:/etc/nginx/conf.d/default.conf:ro
    deploy:
      restart_policy:
        condition: any
    depends_on:
      - frontend
      - backend
    networks:
      - subsora_network

  xray:
    image: ghcr.io/xtls/xray-core:latest
    container_name: subsora-xray
    hostname: xray
    volumes:
      - xray_config:/etc/xray
      - ./xray_logs:/var/log/xray
    ports:
      - target: 443
        published: 443
        protocol: tcp
        mode: host
    environment:
      - XRAY_LOCATION_ASSET=/usr/share/xray/
    deploy:
      restart_policy:
        condition: any
    networks:
      - subsora_network

volumes:
  postgres_data:
    external: true
  xray_config:

configs:
  bot_env_config:
    file: /root/subsora/bot/.env
  backend_env_config:
    file: /root/subsora/.env

networks:
  subsora_network:
    driver: overlay
    attachable: true