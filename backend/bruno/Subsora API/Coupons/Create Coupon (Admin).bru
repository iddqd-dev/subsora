meta {
  name: Create Coupon (Admin)
  type: http
  seq: 1
}

post {
  url: {{base_url}}/coupons/
  body: json
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

body:json {
  {
    "code": "SALE25",
    "discount_percent": 25,
    "max_uses": 100,
    "is_active": true
  }
}
